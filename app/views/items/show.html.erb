<div class="container-fluid">
  <div class="row">
    <div class="col-xs-12 col-lg-4">
      <h1><%= @item.description %></h1>
      <table class="table table-hover">
       <thead>
          <tr>
            <th>Item id</th>
            <th>Description</th>
            <!-- if item is not available -->
            <!-- Add lender -->
            <!-- Add borrower -->
            <!-- else -->
            <!-- available -->
          </tr>
        </thead>
        <tbody>
          <tr>
            <td><%= @item.id %></td>
            <td><%= @item.description %></td>  
            <!-- if item is not available -->
            <!--lender  -->
            <!-- borrow -->
            <!-- else -->
            <!-- available -->
          </tr>
        </tbody>
      </table>

      <a class="btn btn-success largeBtn" href="/items/" role="button">Back</a>
    </div>
  </div>
</div>

